# Core LangChain Libraries
langchain==0.2.5
langchain-core==0.2.9
langgraph==0.0.69

# LLM and Tool Integrations
langchain-openai==0.1.8

# Data models and validation (compatible with LangChain's use of v1 and v2)
pydantic>=1,<3

# For managing environment variables (API keys, endpoints)
python-dotenv==1.0.1
